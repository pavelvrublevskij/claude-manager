---
name: p-run
description: Start the development server
disable-model-invocation: true
---

Run the development server with `node server.js`. If port 3000 is already in use, kill the existing process first.
